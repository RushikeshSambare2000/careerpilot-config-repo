server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:careerpilot_notification}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:cdac}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

# Same shape as the .NET app's "Email" config section (Email:SmtpHost,
# Email:SmtpPort, Email:Username, Email:Password, Email:From), renamed to
# Spring's kebab-case convention. Leave smtp-host/username blank for local
# dev - EmailService then logs + console-prints instead of sending, exactly
# like the original EmailService.cs does. Real credentials belong in
# environment variables, never here - this file lives in a git repo the
# config server reads from.
notification:
  email:
    smtp-host: smtp.gmail.com
    smtp-port: 587
    username: rushikesh.sambare.cmfeb26@gmail.com
    password: regk fvnl xuaq utjp
    from: rushikesh.sambare.cmfeb26@gmail.com

management:
  endpoints:
    web:
      exposure:
        include: health,info
